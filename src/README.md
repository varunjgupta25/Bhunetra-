# Legacy backend archive

`/src` contains the previous RuralRoute TypeScript/Express backend.

Status:
- Archived for reference only
- Not part of Bhunetra active runtime path
- Not included in active CI quality gates

Use `/backend` (FastAPI) as the source of truth for backend development.
